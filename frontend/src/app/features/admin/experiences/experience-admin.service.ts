import { HttpParams } from '@angular/common/http'; import { inject, Injectable } from '@angular/core'; import { map } from 'rxjs'; import { ApiClientService } from '../../../core/api/api-client.service'; import { ApiResponse } from '../../../core/api/api-response.model'; import { requireData } from '../shared/admin-api'; import { Experience, ExperienceRequest, ReorderItem } from '../shared/admin.models';
@Injectable() export class ExperienceAdminService { private api=inject(ApiClientService); list(search?:string){const options=search?{params:new HttpParams().set('search',search)}:{};return this.api.get<ApiResponse<Experience[]>>('admin/experiences',options).pipe(map(requireData));} get(id:string){return this.api.get<ApiResponse<Experience>>(`admin/experiences/${id}`).pipe(map(requireData));} create(body:ExperienceRequest){return this.api.post<ApiResponse<Experience>>('admin/experiences',body).pipe(map(requireData));} update(id:string,body:ExperienceRequest){return this.api.put<ApiResponse<Experience>>(`admin/experiences/${id}`,body).pipe(map(requireData));} delete(id:string){return this.api.delete<void>(`admin/experiences/${id}`);} reorder(items:ReorderItem[]){return this.api.put<ApiResponse<unknown>>('admin/experiences/reorder',{items});} }
