import { Component, input } from '@angular/core';
@Component({ selector: 'app-admin-page-header', template: `<header><div><p>{{ eyebrow() }}</p><h1>{{ title() }}</h1>@if (description()) { <span>{{ description() }}</span> }</div><ng-content /></header>`, styles: `header { display:flex; justify-content:space-between; align-items:end; gap:1rem; margin-bottom:2rem; } p { margin:0; color:var(--color-primary); font-size:.7rem; font-weight:700; letter-spacing:.14em; text-transform:uppercase; } h1 { margin:.35rem 0; font-size:clamp(2rem,5vw,3.2rem); } span { color:var(--color-text-muted); } @media(max-width:600px){header{align-items:flex-start;flex-direction:column}}` })
export class AdminPageHeaderComponent { readonly title=input.required<string>(); readonly eyebrow=input('Admin'); readonly description=input<string|null>(null); }
