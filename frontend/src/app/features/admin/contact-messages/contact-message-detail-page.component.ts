import{DatePipe}from'@angular/common';import{Component,inject,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{ActivatedRoute,RouterLink}from'@angular/router';import{take}from'rxjs';import{LoadingIndicatorComponent}from'../../../shared/components/loading-indicator/loading-indicator.component';import{safeAdminError}from'../shared/admin-api';import{ContactMessage,ContactStatus}from'../shared/admin.models';import{AdminPageHeaderComponent}from'../shared/admin-page-header.component';import{ContactMessageAdminService}from'./contact-message-admin.service';
@Component({selector:'app-contact-message-detail-page',imports:[FormsModule,RouterLink,DatePipe,LoadingIndicatorComponent,AdminPageHeaderComponent],providers:[ContactMessageAdminService],template:`<div class="admin-page"><app-admin-page-header title="Message Detail" eyebrow="Inbox"><a class="admin-button" routerLink="/admin/contact-messages">Back</a></app-admin-page-header>@if(loading()){<div class="state-panel"><app-loading-indicator label="Loading message"/></div>}@else if(error()){<p class="admin-error" role="alert">{{error()}}</p>}@else if(message();as m){<article class="admin-panel message"><header><div><h2>{{m.subject||'No subject'}}</h2><p>From <strong>{{m.name}}</strong> · <a [href]="'mailto:'+m.email">{{m.email}}</a></p></div><span class="status-badge">{{m.status}}</span></header><dl><dt>Received</dt><dd>{{m.receivedAt|date:'medium'}}</dd>@if(m.readAt){<dt>Read</dt><dd>{{m.readAt|date:'medium'}}</dd>}@if(m.repliedAt){<dt>Replied</dt><dd>{{m.repliedAt|date:'medium'}}</dd>}</dl><p class="body">{{m.message}}</p><label>Status <select class="admin-input" [ngModel]="m.status" (ngModelChange)="setStatus($event)">@for(x of statuses;track x){<option [value]="x">{{x}}</option>}</select></label><p class="hint">REPLIED records status only; this screen does not send email.</p></article>}</div>`,styles:`.message{max-width:60rem}.message header{display:flex;justify-content:space-between;gap:1rem}.message h2{margin-top:0}.message a{color:var(--color-primary)}dl{display:grid;grid-template-columns:auto 1fr;gap:.35rem 1rem;color:var(--color-text-muted)}dt{font-weight:700}.body{padding:1.5rem 0;white-space:pre-wrap;overflow-wrap:anywhere;border-block:1px solid var(--color-border)}.hint{color:var(--color-text-dim);font-size:.8rem}`})
export class ContactMessageDetailPageComponent{private api=inject(ContactMessageAdminService);private id=inject(ActivatedRoute).snapshot.paramMap.get('id')!;readonly statuses:ContactStatus[]=['NEW','READ','REPLIED','ARCHIVED'];readonly message=signal<ContactMessage|null>(null);readonly loading=signal(true);readonly error=signal<string|null>(null);constructor(){this.api.get(this.id).pipe(take(1)).subscribe({next:x=>{this.message.set(x);this.loading.set(false)},error:e=>{this.error.set(safeAdminError(e));this.loading.set(false)}})}setStatus(status:ContactStatus){this.api.updateStatus(this.id,status).pipe(take(1)).subscribe({next:x=>this.message.set(x),error:e=>this.error.set(safeAdminError(e))})}}
