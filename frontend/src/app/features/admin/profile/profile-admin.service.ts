import { inject, Injectable } from '@angular/core'; import { map } from 'rxjs'; import { ApiClientService } from '../../../core/api/api-client.service'; import { ApiResponse } from '../../../core/api/api-response.model'; import { requireData } from '../shared/admin-api'; import { AdminProfile, ProfileRequest } from '../shared/admin.models';
@Injectable() export class ProfileAdminService { private api=inject(ApiClientService); get(){return this.api.get<ApiResponse<AdminProfile>>('admin/profile').pipe(map(requireData));} update(body:ProfileRequest){return this.api.put<ApiResponse<AdminProfile>>('admin/profile',body).pipe(map(requireData));} }
